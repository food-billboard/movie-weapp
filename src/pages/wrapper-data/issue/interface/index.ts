import IFormData from './formData'

enum EIndexesType {
  director = 'director',
  actor = 'actor',
  district = 'district',
  // LANGUAGE = 'LANGUAGE',
  // CLASSIFY = 'CLASSIFY'
}

export {
  IFormData,
  EIndexesType
}